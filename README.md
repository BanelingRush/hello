# jkavanagh.dev

Personal site. Single static `index.html`, no build step, no dependencies.

## Preview locally

    python3 -m http.server -d . 8000
    # http://localhost:8000

## Deploy

GitHub Pages serves `main` from `/`. Push to deploy.
